这道题的核心是滑动窗口（双指针）算法。
1.目标：找到字符串中不含重复字符的最长连续子串的长度
2.关键点：
（1）必须是子串（连续），不是子序列
（2）不能有重复字符
3. 核心思路：滑动窗口
想象一个可以伸缩的窗口在字符串上滑动：
窗口：[left, right]
初始状态：
s = "abcabcbb"
     ↑
   left,right
扩展窗口（right右移）：
s = "abcabcbb"
     ↑   ↑
    left right
遇到重复字符时收缩窗口（left右移）：
s = "abcabcbb"
       ↑ ↑
    left right
4. 具体步骤（代码）
（1）定义函数，接收一个字符指针 s（输入字符串）
（2）创建大小为128的整型数组，作为哈希表，用于记录每个ASCII字符最后一次出现的位置索引
（3）遍历哈希表，将所有值初始化为-1
（4）定义左指针，右指针，最长字符串长度，len
（5）主循环，右指针滑动窗口遍历
（6）获取当前位置的字符
（7）检查当前字符上次出现的位置，移动左指针
（8）更新哈希表
（9）计算当前窗口长度：右边界 - 左边界 + 1（闭区间）
（10）动态更新全局最大值
（11）循环结束，返回记录的最长无重复子串长度
